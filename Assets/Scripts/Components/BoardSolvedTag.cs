namespace Scripts.Components
{
    /// <summary>On the board entity from the frame it is solved until the board is destroyed. Gameplay filters exclude it.</summary>
    public struct BoardSolvedTag
    {
    }
}
